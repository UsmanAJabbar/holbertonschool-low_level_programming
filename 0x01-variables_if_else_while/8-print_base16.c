
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
	{
	int l;
	int u;

	for (l = 48; l <= 57; l++)
	{
	putchar(l);
	}
	for (u = 97; u <= 102; u++)
	{
	putchar(u);
	}
	putchar ('\n');
	return (0);
}


