#include "holberton.h"

void printf_c(var_list)
{
	return (write(1, &c, 1));
}

void print_str(var_list)
{
	int index;
	char strcapture;

	if (*s == NULL)
	{
		return(NULL);
	}

	while (s[index]) != '\0')
	{
		strcapture[index] == s[index];
	}
	return(strcapture);
}

void
