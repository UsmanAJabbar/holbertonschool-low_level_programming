#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 *
 * _abs - computs absolute value
 *
 * Return: Always 0.
 */

int _abs(int n)
{
int mbm1 = n * -1;

	if (n < 0)
	{
		return (mbm1);
	}

	else
	{
		return (n);
	}
}
